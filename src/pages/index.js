import React from "react"

export default () => (
    <div style={{ color: `purple` }}>
        <h1>Testing Site</h1>
        <p>This is a test site for the Gatsby Platform using React</p>
    </div>
)
